import React from 'react';
import AnimatedScreen from './util/AnimatedScreen';
import HeaderArrow from '../components/custom/HeaderArrow';

const FavoriteScreen = () => {
  return (
    <AnimatedScreen>
      <HeaderArrow />
    </AnimatedScreen>
  );
};

export default FavoriteScreen;
